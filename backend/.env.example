# Configurações do Servidor
PORT=3001
NODE_ENV=development

# URL do Frontend no Vercel (separar múltiplos por vírgula)
# Exemplo: https://seu-app.vercel.app,https://seu-app-staging.vercel.app
FRONTEND_URL=http://localhost:3000,https://admin.selfmachine.com.br

# OpenAI API Key (para funcionalidades de IA)
# Obtenha em: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-your-api-key-here

# Database (PostgreSQL no Render, SQLite local)
# Em produção, o Render vai fornecer automaticamente
# DATABASE_URL=postgresql://user:password@host:port/database

# Senhas para painéis (obrigatório para produção)
ADMIN_PASSWORD=senha_super_secreta_admin
KITCHEN_PASSWORD=senha_super_secreta_cozinha

# Chave secreta para assinar os tokens JWT (use um gerador de string aleatória)
JWT_SECRET=sua_chave_super_secreta_e_longa_aqui

# Redis para cache distribuído (opcional - se não configurado, usa Map em memória)
# Formato: redis://[username][:password]@host:port
# Exemplo local: redis://localhost:6379
# Exemplo Redis Cloud/Render: redis://red-xxxxx:6379 ou rediss://red-xxxxx:6379 (SSL)
# REDIS_URL=redis://localhost:6379
